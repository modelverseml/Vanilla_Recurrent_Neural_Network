# Full local environment: everything the deployed app needs, PLUS transformers
# (for the BERT encoder), the notebooks, and dataset tooling. Use this to run the
# complete pipeline locally:  pip install -r requirements-dev.txt
-r requirements.txt

transformers
matplotlib
ipykernel
datasets

# Dependencies for the DEPLOYED Streamlit app (Streamlit Community Cloud reads
# this file). The deploy skips BERT (see streamlit_app.py: VRNN_ENCODERS), so
# `transformers` is NOT needed here -- it lives in requirements-dev.txt instead.
# The app runs PyTorch + TensorFlow + the from-scratch NumPy RNN across the three
# static encoders (word2vec / fastText / GloVe).
#
# For the full local pipeline (incl. BERT and the notebooks) use requirements-dev.txt.

streamlit
numpy
pandas
scipy
gensim

# CPU-only PyTorch keeps the build small (no multi-GB CUDA wheels)
--extra-index-url https://download.pytorch.org/whl/cpu
torch

tensorflow
keras

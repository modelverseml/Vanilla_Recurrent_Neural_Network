# Dependencies for the DEPLOYED Streamlit app (Streamlit Community Cloud reads
# this file). It runs the lightweight set that fits the free tier: PyTorch + the
# from-scratch NumPy RNN across the static encoders.
#
# Intentionally NOT here:
#   - tensorflow / keras : heaviest dep, and no wheels on Streamlit's Python
#   - transformers       : only needed for BERT, which the deploy skips
# Both live in requirements-dev.txt for the full local pipeline.

streamlit
numpy
pandas
scipy
gensim

# CPU-only PyTorch keeps the build small (no multi-GB CUDA wheels)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
